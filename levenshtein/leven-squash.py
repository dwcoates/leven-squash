import logging
from Levenshtein import

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)


class LevenSquash:
    def __init__(self):
        pass

    # this exists to provide the flexibility for different LD algorithms
    def get_distance(self):

        return
